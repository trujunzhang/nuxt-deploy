export declare class SocialButtonUtils {
    getConnectionTitle(buttonType: string): "" | "Connect to Facebook" | "Connect to Twitter";
    getLoggedTitle(buttonType: string, typeTitle: string): string;
    getTitle(socialButtonType: string, buttonType: string, typeTitle: string): string;
}

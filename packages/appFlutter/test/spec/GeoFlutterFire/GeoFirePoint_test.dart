// Import the test package and Counter class
import 'package:flutter_test/flutter_test.dart';
// import 'package:geoflutterfire/geoflutterfire.dart';

void main() {
  test('Result should return correctly', () {
    // Geoflutterfire geo = Geoflutterfire();
    // GeoFirePoint myLocation =
    //     geo.point(latitude: 37.388825, longitude: -121.820108);
    // var distance = myLocation.distance(lat: 37.388969, lng: -121.820384);
    // expect(distance == 0.029, true);
    //
    // distance = myLocation.distance(lat: 37.389387, lng: -121.820059);
    // expect(distance == 0.063, true);
  });
}

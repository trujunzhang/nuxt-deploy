class AvatarUser {
  String uid;

  // user(2)
  final String username;
  final String avatarUrl;

  AvatarUser(
      this.uid, // uniqueID
      this.username, // user name
      this.avatarUrl);
}
